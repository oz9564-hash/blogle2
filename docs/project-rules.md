# 프로젝트 협업 규칙

여러 수강생이 같은 레포에서 AI를 통해 협업하기 위한 기본 규칙입니다.

## 목적

- 수강생이 직접 코딩하는 방식이 아니라, AI에게 작업을 맡기는 방식을 정합니다.
- 여러 명이 동시에 기능을 요청해도 공통 구조가 깨지지 않게 합니다.
- PR, 검토, 승인, 병합 기준을 정합니다.

## 기본값

### 역할 분담

- 수강생은 기획, 요구사항, 기준 결정, 결과 검토를 담당합니다.
- AI는 코드 수정, 문서 수정, 테스트 확인, 커밋, PR 생성을 담당합니다.
- 병합 여부는 사람이 최종 결정합니다.

### 작업 단위

작업은 아래 단위 중 하나로 구분합니다.

- 지원자 화면
- 채용담당자 화면
- 면접관 화면
- 공통 헤더와 유저 전환
- 공통 컴포넌트
- 데이터 구조
- Firebase 연결과 배포
- 문서 수정

### 브랜치

- `main` 브랜치는 배포 가능한 상태로 유지합니다.
- AI 작업은 기능 브랜치에서 진행하는 것을 기본으로 합니다.
- 브랜치 이름은 `feature/작업내용`, `fix/작업내용`, `docs/작업내용` 형식을 기본으로 합니다.

### PR

- 모든 기능 브랜치는 `main` 병합 전에 PR을 만듭니다.
- PR 생성은 AI가 수행합니다.
- PR 제목, 설명, 변경 요약, 확인한 명령, 남은 이슈는 AI가 작성합니다.
- PR은 긴 회의용 리뷰 문서가 아니라 `main` 진입 전 체크포인트로 사용합니다.
- 사람은 PR 내용을 검토하고 승인합니다.
- 작은 기능 변경은 AI 확인과 사람 1명 승인 후 병합할 수 있습니다.
- 공통 구조를 건드리는 PR은 최소 1명 이상이 확인합니다.
- 디자인이 약간 어긋나는 정도는 후속 정리 작업으로 남길 수 있습니다.
- 헤더, 유저 전환, 기본 레이아웃처럼 공통 구조를 깨는 변경은 후속 작업으로 미루지 않습니다.

### 병합

- `main` 병합은 승인 후 진행합니다.
- 여러 기능 브랜치를 한 번에 병합하지 않습니다.
- 기능 브랜치가 여러 개 있으면 PR 단위로 하나씩 순서대로 병합합니다.
- 하나의 PR이 병합된 뒤 다음 브랜치를 최신 `main` 기준으로 확인합니다.
- 충돌이 생기면 AI가 해결안을 제시하고, 사람이 확인합니다.
- 공통 헤더, 유저 전환, 데이터 구조, 디자인 시스템 변경은 단독 병합하지 않습니다.

## 논의할 항목

- PR을 반드시 사용할지
- 브랜치 이름 규칙
- PR 승인 인원
- 병합 담당자
- 공통 구조 변경 승인 기준
- 작업 단위를 어떻게 나눌지
- 동시에 같은 파일을 수정할 때의 우선순위
- AI가 커밋과 PR까지 자동으로 진행해도 되는 범위
- 디자인 정리를 별도 후속 작업으로 남길 수 있는 기준
- 기능 브랜치 병합 순서를 누가 정할지

## 최종 결정

- 작업 단위:
- 브랜치 전략:
- PR 사용 여부: 필수
- PR 작성 기준: AI가 제목, 설명, 변경 요약, 확인 명령, 남은 이슈를 작성
- PR 운영 방식: `main` 병합 전 가벼운 체크포인트로 사용
- PR 승인 기준: 작은 기능은 사람 1명 승인, 공통 구조 변경은 추가 확인
- 병합 담당:
- 병합 순서: 기능 브랜치가 여러 개 있으면 하나씩 직렬로 병합
- 공통 구조 변경 기준:
- 충돌 해결 기준:
- AI 커밋/PR 권한: AI가 커밋과 PR 생성을 수행
- 디자인 후속 정리 기준:

## 변경 이력

- 2026-05-29: AI 주도 협업과 PR 운영 기준 반영
- 2026-05-29: 디자인 불일치를 후속 정리 작업으로 다루는 기준 추가
- 2026-05-29: PR 필수, AI 작성, 기능 브랜치 직렬 병합 기준 확정
